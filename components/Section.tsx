import Math2 from "../utils/Math2";

type SectionProps = {
  progress: number;
  et: number;
};

type ReturnProps = {
  position: [number, number, number];

  rotation: [number, number, number];
  rotationLocal: number;
};
